const getDashboardStatsData = async () => {
  // swith case for customer, seller and admin dahsboard stats data
  // for customer -> total orders, total amount spent, total medicines bought
  // for seller -> total orders, total amount earned, total medicines sold
  // for admin -> total users, total sellers, total customers, total orders, total amount earned by the platform
};

export const statsService = {
  getDashboardStatsData,
};
