model Cart {
  id        String   @id @default(uuid(7))
  userId    String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user      User       @relation(fields: [userId], references: [id])
  cartItems CartItem[]

  @@index([userId], name: "cart_user_id_idx")
  @@map("carts")
}
