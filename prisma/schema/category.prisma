model Category {
  id          String  @id @default(uuid(7))
  name        String  @unique
  icon        String?
  description String?

  medicines Medicine[]

  @@index([name], name: "category_name_idx")
  @@map("categories")
}
